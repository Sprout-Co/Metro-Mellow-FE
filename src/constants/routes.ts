export enum Routes {
  // Auth routes
  GET_STARTED = "/get-started",
  LOGIN = "/login",
  REGISTER = "/register",
  FORGOT_PASSWORD = "/forgot-password",

  // App routes
  DASHBOARD = "/dashboard",
  PROFILE = "/profile",
  SETTINGS = "/settings",

  // Service routes
  SERVICES = "/services",
  BOOKINGS = "/bookings",
  PAYMENTS = "/payments",

  // Admin routes
  ADMIN_DASHBOARD = "/admin",
  USERS = "/admin/users",
  STAFF = "/admin/staff",

  // Public routes
  HOME = "/",
  ABOUT = "/about",
  CONTACT = "/contact",
  TERMS = "/terms",
  PRIVACY = "/privacy",
}
