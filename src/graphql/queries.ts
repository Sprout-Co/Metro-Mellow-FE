import { gql } from "@apollo/client";

export const EXAMPLE_QUERY = gql`
  query ExampleQuery {
    # Replace this with an actual query from your schema
    # For example:
    # users {
    #   id
    #   name
    #   email
    # }
  }
`;
